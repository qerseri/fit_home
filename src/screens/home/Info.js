import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

export default Info = () => {
  return (
    <SafeAreaView>
      <Text>Info</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  
});
