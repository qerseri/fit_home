import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  SafeAreaView,
  TouchableOpacity,
} from 'react-native';

export default LoginScreen = () => {
    return (
        <SafeAreaView>
            <Text>login</Text>
        </SafeAreaView>
    );
};

const styles = StyleSheet.create({
  
});